{
  "name": "cosmos-homepage",
  "version": "1.2.0",
  "private": false,
  "type": "module",
  "description": "An interactive starfield homepage for Obsidian vaults.",
  "license": "MIT",
  "main": "main.js",
  "scripts": {
    "build": "node scripts/build.mjs",
    "lint": "eslint src",
    "test": "node --test tests/*.test.mjs",
    "check": "node scripts/check.mjs",
    "bench": "node scripts/bench.mjs",
    "demo": "node scripts/demo-server.mjs",
    "release:verify": "npm run check && npm run lint && npm test && npm run build"
  },
  "devDependencies": {
    "esbuild": "0.28.1",
    "eslint": "^9.39.4",
    "eslint-plugin-obsidianmd": "^0.4.0",
    "globals": "^17.6.0",
    "obsidian": "^1.13.1"
  }
}
